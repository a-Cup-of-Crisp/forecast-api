services:
  client:
    restart: always
    ports:
      - 80:80

  server:
    image: lsdrfrx/forecast-api
    restart: always
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - API_KEY=f0f8250a24f84c1bb693095488b4830a
    ports:
      - 3000:3000

  ai:
    restart: always
    environment:
      - HOST=0.0.0.0
      - PORT=6000
    ports:
      - 6000:6000
